{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test:e2e": "npx nightwatch --config nightwatch.conf.js"
  },
  "devDependencies": {
    "chromedriver": "^110.0.0",
    "nightwatch": "^2.6.17",
    "nuxt": "^3.2.3"
  }
}
